# 更新日志（Changelog）

本项目的全部版本更新内容按时间倒序记录在此文件中（最新版本在最上方）。

- 版本标注：**正式版** 为可发布版本；**测试版（Alpha / Beta）** 为开发中版本，仅供验证，不提供压缩包。
- 格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。
- 最新版本：**[v1.6.2（正式版）](#162正式版)**

---

## [1.6.2]（正式版）

自 v1.6.1 / v1.6.2 测试链验证后正式发布，包含：

- **一级 UI 设置入口**：左下角「⚙ 设置」菜单集中提供「全局默认音量」「关于」「删除用户数据」（二级 UI 工具栏移除对应按钮）
- **添加歌曲「主菜单」状态**：持久化保存，song name 自动锁为 `maintheme` 并带提示，切回其他状态自动恢复缓存
- **电台索引**：任意目录创建的电台均可打开 / 移除 / 编辑位置
- **BOM 自动修复**：打开电台时自动补回 UTF-8 BOM（兜底 Paradox 启动器重写注册文件）
- **删除用户数据**：一键清除软件自身配置，不删除任何 mod / 游戏文件
- **移除「批量添加」入口**：仅删除按钮，相关代码保留

### 1.6.2 beta_2（测试版）

- **修复：主菜单名称提示被截断**——提示文字改为输入框下方独立一行完整显示（不再与 song name 输入框挤占）

### 1.6.2 beta_1（测试版）

- **添加歌曲 song name 智能联动**：输入实时缓存；选「主菜单」时自动把名称改为 `maintheme` 并锁死输入框（名称后显示注释说明），切回其他状态自动恢复缓存内容

## [1.6.1] beta_5（测试版）

- **修复：播放逻辑选「主菜单」无法持久化**——之前主菜单不产生条件，保存后变回「任意播放」；现以哨兵注释 `# maintheme`（游戏按注释忽略）写入 `_songs.txt`，保存后列表与重新编辑均正确显示「主菜单」
- song name 非 `maintheme` 时弹窗提醒（主菜单只播放名为 maintheme 的歌曲）

### 1.6.1 beta_4（测试版）

- **电台索引**：修复在其他目录创建的电台无法被打开——创建/打开电台后自动登记到索引（`%APPDATA%\HoI4MusicEditor\radio_index.json`，记录电台 ID、显示名、目录位置）
- 「打开电台 MOD」合并显示索引电台（任意目录）+ 当前目录扫描，支持**移除索引**（不删电台文件）与**编辑位置**（电台文件夹移动后修正路径）

### 1.6.1 beta_3（测试版）

- **修复：修复后重开仍报「注册文件缺少 UTF-8 BOM」**——根因是 Paradox 启动器每次启动游戏会重写所有 .mod 注册文件（去 BOM、path 改绝对路径）；现在打开电台时自动补回 BOM 并状态栏提示，不再弹问题框
- BOM 检测仅对中文等非 ASCII 显示名报告（纯 ASCII 名无乱码风险）

### 1.6.1 beta_2（测试版）

- **设置新增「删除用户数据」**：一键清除软件自身数据（config.json：上次使用的文件夹 / 全局默认音量 / 列表排序），不删除任何 mod、游戏文件或电台 MOD；删除后当前会话即时恢复默认

### 1.6.1 beta_1（测试版）

- **一级 UI 设置入口**：左下角「⚙ 设置」按钮集成「全局默认音量」「关于」，二级 UI 工具栏移除这两个按钮
- **移除「批量添加」入口**：仅删除按钮，相关代码保留
- **添加歌曲新增「主菜单」状态**：选主菜单时禁用阵营选择与高级自定义（主菜单音乐为引擎固定播放名为 `maintheme` 的歌曲，无阵营/条件）
- **修复：添加歌曲对话框高级编辑区消失**（主菜单联动初始化时机导致构造中断）

## [1.6.0]（正式版）

自 v1.6.0 Beta 链验证后正式发布，包含：

- **开局音效（start_game）功能**：音频转 WAV（44.1kHz/16bit/立体声）→ 单条双端区间条裁剪（≤15 秒，超限拦截）→ 无延迟试听 → 末尾淡出（0.5~8s 可设）→ 音量 → 一键安装
- 安装采用**追加定义文件** `sound/zz_<电台>_sounds.asset` + `start_game_01`（点开始游戏）/ `start_game_02`（进入游戏世界）双音效替换，**不顶掉原版音效**；支持状态显示与一键禁用
- 「修复格式」可检测并迁移历史遗留的误用 `sound.asset`

### 1.6.0 Beta_7（测试版）

- **开局音效 01 + 02 双替换**：start_game_01（点开始游戏）与 start_game_02（进入游戏世界）都替换为同一段自定义音频，进世界不再无声
- **修复：定义文件误用 sound.asset 会顶掉原版全部音效**——改为追加定义文件 `sound/zz_<电台>_sounds.asset`（游戏对 sound.asset 是整文件替换语义）；一键修复可自动检测并迁移历史遗留的 sound.asset

### 1.6.0 Beta_6（测试版）

- **开局音效状态与信息显示**：打开对话框即显示当前状态（已安装 / 未安装）及已安装音效的时长、音量
- **禁用开局音效按钮**：一键删除电台 mod 内的 sound/sound.asset 与 start_game_01.wav，恢复游戏默认开局音效（不动游戏本体）

### 1.6.0 Beta_5（测试版）

- **末尾淡出可选项**：勾选「最后几秒淡出」后，音效结尾音量线性渐降到 0，淡出时长可设（0.5~8 秒）；试听与安装同步生效
- **时长上限 12s → 15s**

### 1.6.0 Beta_4（测试版）

- **区间条自由拖拽**：结束手柄不再被 12s 上限卡住，可一路拖到音频结尾；**超过 12 秒区间变红并提示「将无法安装」**，安装时拦截并提示缩小区间
- 手柄命中区域加宽（±12px）拖动更跟手

### 1.6.0 Beta_3（测试版）

- **试听/安装改为无延迟切片**：选文件时已生成 44.1kHz 中间 WAV，试听与安装改为纯 Python 直接切片（实测 ~1ms），不再每次调用 ffmpeg 转换，点「试听」即时播放

### 1.6.0 Beta_2（测试版）

- **开局音效裁剪条重做**：两个独立滑块改为**单条双端区间条**——一条轨道上红色手柄=截开头、绿色手柄=截结尾，中间绿色区间即保留片段，实时显示起止时间与时长（≤12 秒）
- **新增实时试听**：裁剪时点「试听」直接播放当前区间片段（自动转 WAV 播放，再点停止；拖动区间自动停止），调整到满意再安装

### 1.6.0 Beta_1（测试版）

- **开局音效（start_game）功能**（本地电台模式下）：音频转 WAV（44.1kHz/16bit/立体声）→ 区间裁剪（限 12 秒）→ 音量调节 → 一键安装进电台 mod（自动写 `sound/sound.asset` 索引 + 复制 `sound/menu/start_game_01.wav`），不改动游戏本体
- **修复：radio 模式工具栏溢出**——新增「开局音效」按钮后工具栏过挤导致「更换图标」被压缩、新按钮被隐藏；编辑模式标签移入独立路径行，工具栏按钮完整显示

## [1.5.0]（正式版）

自 v1.4 测试链验证后正式发布，包含：

- **本地电台 MOD**：一键生成完整独立电台 MOD（注册文件 + descriptor + gfx 图标 + interface 电台界面 + music 歌曲目录），不修改游戏本体与已有 mod
- **两级界面**：启动选择「本地电台编辑」/「已有电台编辑」，二级 UI 常驻显示当前编辑模式（工具栏标签 + 窗口标题）
- **电台图标**：304×120 横向两帧权威尺寸；拖拽裁剪编辑器（152:120 比例 + 两帧实时预览）；更换图标自动加红/绿边框（未选中/选中）
- **电台元数据**：`descriptor.mod` 与注册文件以 UTF-8 BOM 写入，中文电台名在游戏内正常显示；`supported_version` 默认合法格式 `1.19.*`
- **电台界面模板**：只引用游戏原版 sprite/字体（`hoi_20b`/`hoi_18b`），不再引用其它 mod 自定义资源导致面板渲染失败
- **一键修复**：「修复格式」在电台模式升级为电台问题排查 + 一键修复（gui 资源 / supported_version / 图标尺寸 / UTF-8 BOM / 缺失文件补生成），修复后自动复查，剩余问题明确提示
- **模式隔离**：本地电台与已有电台的记忆分开存储、切换模式清空残留数据，互不串扰

### 1.4.2 beta.5（测试版）

- **二级界面显示当前编辑模式**：进入「本地电台编辑」/「已有电台编辑」后，工具栏常驻绿色模式标签并在窗口标题标注，随时知道正在用哪种编辑模式
- **一键修复后复查**：修复完成后自动重新排查，剩余问题明确提示（文件被占用/权限不足等），不再出现「修了仍报缺 UTF-8 BOM」的困惑；缺失的注册文件 / descriptor.mod 也会一并补生成
- **更换图标时两帧加边框**：左帧（未选中）红色边框、右帧（选中）绿色边框，宽 2px，游戏内状态一目了然

### 1.4.2 beta.4（测试版）

- **电台模板修复**：生成的电台界面（.gui）不再引用其它 mod 的自定义资源——删除不存在的 `GFX_musicplayer_head` sprite，字体改为游戏原版 `hoi_20b` / `hoi_18b`（否则游戏内面板渲染失败）；`supported_version` 默认改为合法格式 `1.19.*`（`1.*` 会被启动器判为非法）
- **电台一键修复（「修复格式」升级）**：打开电台时自动排查并提示问题——界面引用非原版资源 / supported_version 非法 / 图标尺寸非 304×120 / 元数据缺 UTF-8 BOM；点「修复格式」一键修复全部问题（自动备份 .bak）。旧版生成的电台（Test_Radio 等）可直接一键修复，无需重建

### 1.4.2 beta.3（测试版）

- **修复：游戏内电台图标显示异常**——图标改为原版 `base_game_album_art.dds` 权威尺寸 **304×120 横向两帧**（每帧 152×120，左=未选中、右=选中高亮），裁剪编辑器比例同步调整为 152:120
- **修复：自定义电台游戏内名称显示为 ??????_**——`descriptor.mod` 与注册 `.mod` 文件改为 **UTF-8 BOM** 编码写入（Paradox 启动器无 BOM 时无法解析中文等非 ASCII 显示名）

### 1.4.2 beta.2（测试版）

- **修复：切换编辑模式后 music 数据残留**——从电台模式切到已有模式（或反向）时，歌曲列表仍显示上一个模式的歌曲；现在切换模式会彻底清空编辑状态，各模式只显示并自动恢复自己记忆的 music 内容

### 1.4.2 beta（测试版）

- **本地电台与已有电台的 music 文件夹选择分开**：电台模式与已有模式各自的「上次使用」记忆独立存储（电台记 `last_radio_*`，已有模式记 `last_folder`），互不污染；进入电台模式自动恢复上次电台，进入已有模式恢复上次文件夹
- 修复：切换模式不再串加载对方的 music 目录

## [1.4.1]（修复版）

- **修复：新建电台后工具栏按钮消失**——超长路径文本撑爆工具栏，导致 Tk pack 将「新建/打开电台 MOD」「更换图标」按钮挤出窗口；现电台信息与路径改为独立一行显示（超长自动省略），工具栏按钮始终完整可见
- **修复：打开已有电台时歌曲列表为空（music 消失）**——原实现只认 `music/<电台ID>/` 标准结构，音乐文件位于 `music/` 根目录（手工搭建/其他工具生成的电台）时定位失败、静默显示空列表；现自动回退到 `music/` 根目录按文件组定位，找不到音乐文件时明确提示

## [1.4.0]（正式版）

- **两级界面**：启动先进模式选择页——「本地电台编辑」/「已有电台编辑」，中途可随时切换
- **新增独立本地电台 MOD 功能**：一键生成完整电台 MOD（注册文件 + descriptor + gfx 图标 + interface 电台界面 + music 歌曲目录），不修改游戏本体与已有 mod；可打开已有电台继续编辑
- **电台图标裁剪编辑器**：拖拽选区（锁定 162:130 比例）+ 两帧实时预览，自动合成 162×260 两帧图标；程序生成占位图标，可随时「更换图标」
- 新依赖：Pillow（图标处理，随 exe 打包）

## [1.3.0]（正式版）

- **修复批量添加/多文件转换只添加 1 个的严重 bug**：转换结果收集改为跨轮次累积（根因：轮询回调每轮新建局部列表，导致仅保留最后一轮取到的 1 条结果）
- 转换输出解码容错（UTF-8 + errors=replace），避免中文系统下个别文件转换异常
- 转换缓存目标重名冲突处理（不同目录同名文件批量添加不再互相覆盖）

## [1.2.4]

- 修复批量添加可能只成功部分文件的问题：ffmpeg 输出解码改为 UTF-8 容错（中文系统下部分文件转换会因 GBK 解码崩溃而失败）；批量转换时同名目标文件不再互相覆盖（自动加 `_2` 后缀）

## [1.2.3]

- **多选删除**：Ctrl/Shift 多选条目批量删除（多选时编辑按钮禁用，单选仍可编辑），删除可选一并移除 ogg 文件
- **拖放添加**：把音乐文件（ogg/mp3/flac/wav/m4a/aac）直接拖入程序窗口即可添加
- **批量添加**：独立「批量添加」对话框，一次添加多个文件并统一设置音量/播放逻辑，song name 自动按文件名生成

## [1.2.2]

- 添加歌曲时 song name 适配文件中文原文：中文等 unicode 字符保留（如 `边境1.mp3` → song name `边境1`），不再被转成下划线

## [1.2.1]

- **写回格式对齐标准格式**（参考 pla_music.asset / pla_songs.txt）：每个 music 块之间空一行、规范缩进、闭合行保留尾随 tab
- 新增「修复格式」按钮：一键把错误格式（其他工具/旧版本生成）重建为标准格式（注释与 music_station 保留，自动备份 .bak）
- 修复音频转换：转换命令加 `-vn` 丢弃视频流/封面流，避免源文件含视频轨（如从视频网站下载改名成 mp3 的文件）时转换出的 ogg 混入画面

## [1.2.0]

- **新增音频转换**：mp3 / flac / wav / m4a / aac 一键转换为 ogg 后自动添加（内置 ffmpeg 7.1，转换质量 libvorbis q5 ≈ 160kbps，接近原版风格）
- 转换在后台线程进行，状态栏实时显示进度（如「正在转换 1/3...」）；转换失败单独提示，不影响其他文件
- 转换产物暂存缓存目录（`%TEMP%\hoi4music_cache`）：**点「保存」时才复制进音乐文件夹**，关闭程序自动清理缓存；转换完成时提示用户
- 添加歌曲的文件选择框支持 ogg / mp3 / flac 等多格式
- 新增「打开所在文件夹」按钮与右键菜单：选中歌曲可直接在资源管理器中定位其音频文件（文件缺失时自动打开所在文件夹；未保存的转换文件定位到缓存）
- 新增「关于」对话框（软件名/版本/作者 BulkCooper/QQ交流群 1076344041）；exe 附加 Authenticode 自签名数字签名（减少杀软误报）
- 支持 mod 自定义音乐文件名（如 `xxx_music.asset` + `xxx_songs.txt`）：自动识别文件组并弹框选择，记住上次选择
- 写回格式对齐标准（参考 pla 格式）：每个 music 块之间空一行、规范缩进；新增「修复格式」按钮一键将错误格式重建为标准格式（自动备份 .bak）

## [1.1.0]

- **新增设置持久化**：默认音量 / 上次使用的文件夹 / 列表排序状态自动保存到 `%APPDATA%\HoI4MusicEditor\config.json`，下次启动自动恢复并直接加载上次的文件夹
- 音量改为滑块调节 + 实时数值显示（精确到 0.05），彻底避免缩放/主题下的数值显示裁切问题
- 修复 DPI 缩放下数值显示被裁切的问题（对话框显式尺寸）

## [1.0.0]

- **首个可用版本**
- 核心功能：选择文件夹、歌曲列表（表头排序 ▲/▼ + 搜索）、添加 / 编辑 / 删除歌曲、播放逻辑预设与高级自定义、全局默认音量、保存自动备份 `.bak` 与保存前校验
- 界面：所有窗口屏幕居中（多显示器/小屏适配）、排序三角指示、标题栏版本号、logo 图标（窗口 + exe）
